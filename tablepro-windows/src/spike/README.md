# Phase 2 spike — Tauri Channel<T> throughput

Throwaway code. Paste `runSpike()` into browser devtools console after `import('./spike/spike-channel')` to measure throughput. Scheduled for deletion after Phase 2 fan-out completes.
