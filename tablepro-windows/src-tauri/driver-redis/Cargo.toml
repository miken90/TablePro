[package]
name = "driver-redis"
version = "0.1.0"
edition = "2021"

[dependencies]
tablepro-plugin-sdk = { path = "../plugin-sdk" }
redis = { version = "0.27", features = ["tls-rustls"] }
serde_json = "1"

[lib]
name = "driver_redis"
crate-type = ["cdylib"]
