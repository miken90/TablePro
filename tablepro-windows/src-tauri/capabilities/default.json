{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window",
  "windows": ["welcome", "main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
