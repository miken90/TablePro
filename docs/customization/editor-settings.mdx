---
title: Editor Settings
description: SQL editor font, size, word wrap, tab width, and Vim mode configuration
---

# Editor Settings

Configure the SQL editor in **Settings** > **Editor**.

## Font Settings

### Font Family

Available monospace fonts:

| Font | Description |
|------|-------------|
| **Consolas** | Classic Windows monospace (default) |
| **JetBrains Mono** | -- |
| **Fira Code** | Ligature-friendly monospace |
| **Monaco** | Traditional Mac programming font (also installable on Windows) |
| **monospace** | Generic OS monospace fallback |

The editor's font CSS stack is `<selected font>, Consolas, 'Courier New', monospace`, so if a selected font isn't installed, TablePro falls back to Consolas, then Courier New, then the browser's generic `monospace`.

### Font Size

| Setting | Range | Default |
|---------|-------|---------|
| Font Size | 10-24 pt | 14 pt |

## Display Settings

### Word Wrap

A toggle exists in **Settings** > **Editor** (default off) and the value persists, but it is not currently wired to the SQL editor -- toggling it does not change line-wrap behavior in the editor.

### Vim Mode

| Option | Description |
|--------|-------------|
| **Off** | Standard editing keybindings (default) |
| **On** | Modal Vim keybindings -- see [Keyboard Shortcuts](/features/keyboard-shortcuts#vim-mode) |

## Indentation Settings

### Tab Width

A selector exists in **Settings** > **Editor** for 2, 4, or 8 spaces (default 4) and the value persists, but it is not currently wired to the SQL editor -- changing it does not change indentation behavior in the editor.

## Editor Behavior

### Autocomplete

Autocomplete is always on. See [Autocomplete](/features/autocomplete) for details.

Font, font size, and Vim mode apply immediately with no restart required.

## Keyboard Shortcuts

| Action | Shortcut |
|--------|----------|
| Run query | `Ctrl+Enter` |
| Run all queries | `Ctrl+Shift+Enter` |
| Format SQL | `Ctrl+Shift+F` |
| Explain query | `Ctrl+Shift+X` |
| Toggle comment | `Ctrl+/` |
| Select next occurrence | `Ctrl+D` |
| Find | `Ctrl+F` |
| Undo | `Ctrl+Z` |
| Select All | `Ctrl+A` |
