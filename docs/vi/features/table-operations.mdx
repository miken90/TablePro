---
title: Thao tác với Bảng
description: Chỉnh sửa và quản lý các bảng trong cơ sở dữ liệu
---

# Thao tác với Bảng

TablePro cung cấp bộ công cụ để chỉnh sửa và quản lý các bảng cơ sở dữ liệu thông qua giao diện trực quan. Thực hiện các thao tác phổ biến như xóa, truncate và sao chép — tất cả mà không cần viết SQL thủ công.

## Tạo Bảng

Tạo bảng mới bằng giao diện trực quan mà không cần viết SQL:

1. Click chuột phải trong sidebar và chọn **Create New Table...**
2. Hoặc vào **File** > **New Table...**
3. Định nghĩa các cột, kiểu dữ liệu, ràng buộc và khóa trong trình soạn thảo trực quan
4. Click **Create** để thực thi câu lệnh `CREATE TABLE` được tạo

## Các Thao Tác với Bảng

Click chuột phải vào bảng trong sidebar để truy cập các thao tác này.

### Xóa Bảng (Drop Table)

Xóa vĩnh viễn một bảng và toàn bộ dữ liệu của nó.

1. Click chuột phải vào bảng (hoặc chọn nhiều bảng)
2. Chọn **Delete**
3. Cấu hình các tùy chọn trong hộp thoại xác nhận
4. Click **OK** để thực thi

{/* Screenshot: Drop table confirmation dialog */}
<Frame caption="Xác nhận xóa bảng với các tùy chọn">
  <img
    className="block dark:hidden"
    src="/images/drop-table-dialog.png"
    alt="Drop table dialog"
  />
  <img
    className="hidden dark:block"
    src="/images/drop-table-dialog-dark.png"
    alt="Drop table dialog"
  />
</Frame>

**Các tùy chọn theo database:**

<Tabs>
  <Tab title="MySQL/MariaDB">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Tạm thời vô hiệu hóa kiểm tra foreign key |
    | **Cascade** | Không được hỗ trợ |
  </Tab>
  <Tab title="PostgreSQL">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Không được hỗ trợ (sử dụng CASCADE thay thế) |
    | **Cascade** | Xóa tất cả các đối tượng phụ thuộc (views, foreign keys) |
  </Tab>
  <Tab title="SQLite">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Tạm thời vô hiệu hóa kiểm tra foreign key |
    | **Cascade** | Không được hỗ trợ |
  </Tab>
</Tabs>

<Warning>
Xóa bảng là không thể hoàn tác. Luôn sao lưu dữ liệu quan trọng trước khi xóa bảng.
</Warning>

### Truncate Bảng

Xóa tất cả các hàng khỏi bảng trong khi vẫn giữ cấu trúc bảng.

1. Click chuột phải vào bảng (hoặc chọn nhiều bảng)
2. Chọn **Truncate**
3. Cấu hình các tùy chọn trong hộp thoại xác nhận
4. Click **OK** để thực thi

**Các tùy chọn theo database:**

<Tabs>
  <Tab title="MySQL/MariaDB">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Tạm thời vô hiệu hóa kiểm tra foreign key |
    | **Cascade** | Không được hỗ trợ cho TRUNCATE |
  </Tab>
  <Tab title="PostgreSQL">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Không được hỗ trợ (sử dụng CASCADE thay thế) |
    | **Cascade** | Truncate tất cả các bảng được liên kết bởi foreign keys |
  </Tab>
  <Tab title="SQLite">
    | Tùy chọn | Mô tả |
    |----------|-------|
    | **Ignore foreign key checks** | Tạm thời vô hiệu hóa kiểm tra foreign key |
    | **Cascade** | Không được hỗ trợ |
  </Tab>
</Tabs>

<Note>
Truncate nhanh hơn `DELETE FROM table` vì nó không tạo các log xóa từng hàng riêng lẻ. Tuy nhiên, nó reset các bộ đếm auto-increment.
</Note>

{/* Screenshot: Xác nhận truncate bảng với các tùy chọn */}
<Frame caption="Xác nhận truncate bảng với các tùy chọn">
  <img
    className="block dark:hidden"
    src="/images/truncate-table-dialog.png"
    alt="Xác nhận truncate bảng với các tùy chọn"
  />
  <img
    className="hidden dark:block"
    src="/images/truncate-table-dialog-dark.png"
    alt="Xác nhận truncate bảng với các tùy chọn"
  />
</Frame>

### Thao Tác Hàng Loạt

Bạn có thể chọn nhiều bảng trong sidebar và áp dụng Drop hoặc Truncate cho tất cả cùng một lúc. Các tùy chọn giống nhau được áp dụng cho mọi bảng được chọn.

1. Giữ `Cmd` và click để chọn nhiều bảng
2. Click chuột phải và chọn **Delete** hoặc **Truncate**
3. Cấu hình các tùy chọn một lần trong hộp thoại
4. Tất cả các bảng được chọn sẽ được xử lý

## Quản Lý View

Database view là các bảng ảo được định nghĩa bởi câu lệnh SQL. TablePro hiển thị view trong sidebar với biểu tượng mắt và màu tím.

### Tạo View

Tạo view mới bằng cách mở tab SQL editor với template:

1. Click chuột phải trong sidebar và chọn **Create New View...**
2. Hoặc vào **File** > **New View...**
3. Tab SQL editor mới mở với template CREATE VIEW
4. Sửa tên view và câu lệnh SELECT
5. Thực thi query để tạo view

Template tự động thích ứng với loại database:

<Tabs>
  <Tab title="MySQL/MariaDB">
    ```sql
    CREATE VIEW view_name AS
    SELECT column1, column2
    FROM table_name
    WHERE condition;
    ```
  </Tab>
  <Tab title="PostgreSQL">
    ```sql
    CREATE OR REPLACE VIEW view_name AS
    SELECT column1, column2
    FROM table_name
    WHERE condition;
    ```
  </Tab>
  <Tab title="SQLite">
    ```sql
    CREATE VIEW IF NOT EXISTS view_name AS
    SELECT column1, column2
    FROM table_name
    WHERE condition;
    ```
  </Tab>
</Tabs>

{/* Screenshot: Tạo View với template SQL */}
<Frame caption="Tạo View với template SQL">
  <img
    className="block dark:hidden"
    src="/images/create-view-editor.png"
    alt="Tạo View với template SQL"
  />
  <img
    className="hidden dark:block"
    src="/images/create-view-editor-dark.png"
    alt="Tạo View với template SQL"
  />
</Frame>

### Chỉnh Sửa Định Nghĩa View

Sửa đổi định nghĩa SQL của view hiện có:

1. Click chuột phải vào view trong sidebar
2. Chọn **Edit View Definition**
3. Định nghĩa view hiện tại được tải và mở trong tab SQL editor mới
4. Sửa đổi query và thực thi để cập nhật view

<Note>
Với MySQL/MariaDB, bạn có thể dùng `ALTER VIEW` để sửa view. Với PostgreSQL, dùng `CREATE OR REPLACE VIEW`. SQLite không hỗ trợ ALTER VIEW — bạn phải drop và tạo lại view.
</Note>

### Xóa View

Xóa view khỏi database:

1. Click chuột phải vào view trong sidebar
2. Chọn **Drop View**
3. Xác nhận xóa

<Tip>
View không lưu trữ dữ liệu, nên xóa view chỉ xóa định nghĩa view — dữ liệu bảng gốc vẫn nguyên vẹn.
</Tip>

### Khác Biệt Context Menu

Context menu sidebar thay đổi khi bạn click chuột phải vào view:

| Hành động | Bảng | View |
|--------|--------|-------|
| Show Structure | Có | Có |
| Export | Có | Có |
| Import | Có | Không |
| Truncate | Có | Không |
| Edit Definition | Không | Có |
| Delete / Drop | Có | Có |

{/* Screenshot: Tùy chọn context menu riêng cho View */}
<Frame caption="Tùy chọn context menu riêng cho View">
  <img
    className="block dark:hidden"
    src="/images/view-context-menu.png"
    alt="Tùy chọn context menu riêng cho View"
  />
  <img
    className="hidden dark:block"
    src="/images/view-context-menu-dark.png"
    alt="Tùy chọn context menu riêng cho View"
  />
</Frame>

## Thao Tác với Database

### Tạo Database

Tạo database mới trực tiếp từ UI (MySQL/MariaDB):

<Steps>
  <Step title="Mở Database Switcher">
    Click tên database trong thanh công cụ hoặc nhấn `Cmd+K` để mở database switcher
  </Step>
  <Step title="Click Create">
    Click nút **Create** trong thanh công cụ của database switcher
  </Step>
  <Step title="Cấu hình">
    Nhập tên, chọn character set và collation
  </Step>
  <Step title="Tạo">
    Click **Create** để thực thi
  </Step>
</Steps>

{/* Screenshot: Create database dialog */}
<Frame caption="Hộp thoại tạo database">
  <img
    className="block dark:hidden"
    src="/images/create-database.png"
    alt="Create Database"
  />
  <img
    className="hidden dark:block"
    src="/images/create-database-dark.png"
    alt="Create Database"
  />
</Frame>

Các character sets và collations có sẵn:

| Charset | Collations |
|---------|------------|
| **utf8mb4** | utf8mb4_unicode_ci, utf8mb4_general_ci, utf8mb4_bin |
| **utf8** | utf8_unicode_ci, utf8_general_ci, utf8_bin |
| **latin1** | latin1_swedish_ci, latin1_general_ci, latin1_bin |
| **ascii** | ascii_general_ci, ascii_bin |

### Database Switcher

Chuyển đổi nhanh giữa các database trên cùng một kết nối:

1. Click tên database trong thanh công cụ
2. Duyệt danh sách các database có sẵn với metadata
3. Tìm kiếm theo tên để lọc
4. Double-click hoặc chọn và click **Open** để chuyển

Database switcher hiển thị các database gần đây để truy cập nhanh và hỗ trợ làm mới danh sách database.

Với kết nối PostgreSQL, database switcher cũng cho phép bạn duyệt và chuyển đổi giữa các schema (ví dụ: `public`, `auth`, hoặc schema tùy chỉnh). Chuyển đổi schema hoạt động song song với chuyển đổi database — chọn database trước, sau đó chọn schema để duyệt các bảng và view của nó.

{/* Screenshot: Bộ chuyển đổi database với tìm kiếm và database gần đây */}
<Frame caption="Bộ chuyển đổi database với tìm kiếm và database gần đây">
  <img
    className="block dark:hidden"
    src="/images/database-switcher.png"
    alt="Bộ chuyển đổi database với tìm kiếm và database gần đây"
  />
  <img
    className="hidden dark:block"
    src="/images/database-switcher-dark.png"
    alt="Bộ chuyển đổi database với tìm kiếm và database gần đây"
  />
</Frame>

## Thao tác Collection MongoDB

Các thao tác collection MongoDB khác với thao tác bảng SQL ở một số điểm.

### Tạo Collection

Tạo collection MongoDB bằng trình soạn MQL. Để tạo collection trống hoặc có tùy chọn cụ thể (capped, validator):

```javascript
db.createCollection("myCollection", {
  capped: true,
  size: 1048576,
  max: 1000
})
```

### Xóa Collection

Nhấp chuột phải vào collection trong thanh bên và chọn **Drop Table** để xóa. Thao tác này thực thi `db.collection.drop()` và xóa tất cả tài liệu và index.

### Nhân bản Collection

TablePro nhân bản collection MongoDB bằng aggregation pipeline với giai đoạn `$out`, sau đó tạo lại tất cả index trên collection mới.

### Hiển thị Tất cả Collections

Chọn **Show All Tables** từ thanh bên để tự động thực thi `listCollections` và hiển thị tất cả collection trong cơ sở dữ liệu hiện tại.

<Note>
**Truncate** không khả dụng cho các collection MongoDB. Để xóa tất cả tài liệu mà vẫn giữ collection, sử dụng `db.collection.deleteMany({})` trong trình soạn MQL.
</Note>

## Phím Tắt

| Thao tác | Phím tắt |
|----------|----------|
| Xóa bảng đã chọn | `Cmd+Delete` |
| Copy tên bảng | `Cmd+C` (trong sidebar) |
| Export bảng | `Cmd+E` (trong sidebar) |
| Import dữ liệu | `Cmd+I` (trong sidebar) |

## Bước Tiếp Theo

<CardGroup cols={2}>
  <Card title="Cấu trúc Bảng" icon="table-columns" href="/vi/features/table-structure">
    Xem và chỉnh sửa schema của bảng hiện có
  </Card>
  <Card title="Bảng Dữ Liệu" icon="table" href="/vi/features/data-grid">
    Xem và chỉnh sửa dữ liệu bảng
  </Card>
  <Card title="Trình soạn thảo SQL" icon="code" href="/vi/features/sql-editor">
    Viết các truy vấn DDL và DML
  </Card>
  <Card title="Import & Export" icon="file-import" href="/vi/features/import-export">
    Import và export dữ liệu bảng
  </Card>
</CardGroup>
